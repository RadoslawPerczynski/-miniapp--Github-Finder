const searchInput = document.getElementById('searchUser');
searchInput.addEventListener('input', () => {
  if(this.value !== '') {
    //console.log('fetching' + searchInput.value)
  } else {
    //showing error
  }
})
